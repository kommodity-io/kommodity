---
description:
globs: *.go
alwaysApply: false
---

- Prefer structs with methods over plain functions
- Avoid pointers for simple types such as strings or booleans
- Prefer use pointers for structs
- Always end comments with a dot
- Always wrap errors using `fmt.Errorf` in the format of `failed to {{ action }}: %w`
